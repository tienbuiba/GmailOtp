import * as actionTypes from '../actions/Actions'

export const showNoticeModal = () => ({ type: actionTypes.SHOW_NOTICE_MODAL});
export const closeNoticeModal = () => ({ type: actionTypes.CLOSE_NOTICE_MODAL });

